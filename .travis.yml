language: java
jdk:
  - openjdk11

branches:
  only:
    - master

before_script:
  - cd backend
  - cd project

# Travis CI 서버의 HOME
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

before_install:
 - chmod +x gradlew

script: "./gradlew clean build"

# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - fkam12@naver.com


